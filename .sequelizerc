require("@babel/register");

const path = require("path");

module.exports = {
  config: path.resolve("src/setup/Database", "config.js"),
  'models-path': path.resolve('src', 'models'),
  "migrations-path": path.resolve("src", "database", "migrations"),
};